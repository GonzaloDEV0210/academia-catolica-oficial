let scroll = new SmoothScroll('a[href*="nosotros"]', {
    speed: 800,
    offset: 130,
});

let scroll_header = new SmoothScroll('a[href*="header"]', {
    speed: 800,
    offset: 0,
});

let scroll_noticia = new SmoothScroll('a[href*="noticias"]', {
    speed: 800,
    offset: 130,
});

let scroll_galeria = new SmoothScroll('a[href*="galeria"]', {
    speed: 800,
    offset: 130,
});

let talleres_costos = new SmoothScroll('a[href*="talleres_costos"]', {
    speed: 800,
    offset: 130,
});